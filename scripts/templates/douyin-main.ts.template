/**
 * Douyin Mini Game Entry Template
 * Auto-generated by build script
 */
import { GameApplication } from '@ksgames26/core';
import { DouyinAdapter } from '@ksgames26/platform-douyin';

// Import game modules
import '../../src/main';

async function main() {
  const adapter = new DouyinAdapter();

  const game = new GameApplication();
  await game.init({
    platformAdapter: adapter,
    debug: false,
  });

  // Canvas is created by Douyin runtime
  const canvas = adapter.createCanvas();

  // Your game logic here
  console.log('[Douyin] Game initialized');
}

main().catch(console.error);
