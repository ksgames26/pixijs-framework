/**
 * WeChat Mini Game Entry Template
 * Auto-generated by build script
 */
import { GameApplication } from '@ksgames26/core';
import { WechatAdapter } from '@ksgames26/platform-wechat';

// Import game modules
import '../../src/main';

async function main() {
  const adapter = new WechatAdapter();

  const game = new GameApplication();
  await game.init({
    platformAdapter: adapter,
    debug: false,
  });

  // Canvas is created by WeChat runtime
  const canvas = adapter.createCanvas();

  // Your game logic here
  console.log('[WeChat] Game initialized');
}

main().catch(console.error);
